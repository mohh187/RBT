> **حالة التنفيذ — 2026-07-28.** المرحلتان 0 و1 والمرحلة 3 **مبنيّة ومختبَرة ومنشورة**:
> `functions/spend.js` (المقياس + قاطع الميزانية + التجميع + حزم الرصيد)،
> `functions/spend.test.js` (67 اختباراً)، `src/lib/spend.js` + شاشة `/platform/spend`
> + محرّر السقوف في `VenueDetail.jsx` + عدّاد وشراء في `/admin/billing`، وقواعد `firestore.rules`.
>
> **كل قناة تنفق مالاً صارت مقيسة (ثماني قنوات).** العدّادات الثلاثة المتفرقة
> (`ar3dMonthly` و`generateTableImage` و`dinerAiMonthly`) هُجِّرت إلى `takeSpend`، فصارت
> تتبع الباقة، وتظهر في لوحة المنصة، وتقبل الشراء — وكانت قبلها ثوابت مخفية داخل دوالها.
>
> **الشراء المباشر يعمل:** `createPayIntent` بنوع `spendPack`، السعر من جدول الخادم
> (`SPEND_PACKS` أو تعديله في `platformConfig/spendPacks`) ولا يُقرأ أي مبلغ من العميل،
> والـ webhook يضيف الرصيد إلى `tenant.spendExtra.<channel>` فوراً ويُصدر فاتورة مدفوعة
> في الكونسول.
>
> **تصحيح دلالي مهم:** الرصيد المشترى صار **يُستهلك**. السلوك القديم لـ `aiExtra` كان
> يضيف الكمية المشتراة إلى السقف **كل شهر إلى الأبد** — تشتري 1000 طلب مرة فتحصل على
> 1000 إضافي شهرياً مدى الحياة. الآن الرصيد يُخصم مع الاستخدام، فحزمة 1000 تعني 1000.
> هذا يقلّل ما يحصل عليه من اشترى سابقاً، وهو مقصود: «حزمة رصيد» لا تعني اشتراكاً دائماً.
>
> **فرق مقصود عن الجدول 3.1:** السقوف المشحونة أعلى مما يقترحه هذا المستند، لأن 3.1
> يضبط السقف عند **متوسط** استهلاك المنشأة — وحاجز يقطع نصف منشآت الباقة أسوأ من غيابه.
> بدل قاعدة «مجموع كل السقوف أقل من سعر الباقة» (وهي تفترض أن منشأة تستنفد القنوات
> الثماني في شهر واحد، ولا تفعل)، يتحقق `spend.test.js` من الخطرين الحقيقيين:
> **لا قناة واحدة تستطيع إفلاس الباقة وحدها**، و**القنوات التلقائية** (المعاملات + البريد
> + مساعد الضيف — التي تعمل بلا ضغطة إنسان، أي التي يستنزفها الخلل) **تبقى داخل سعر الباقة**.
>
> **السقوف وحزم الشراء المشحونة فعلياً:**
>
> | القناة | menu | ops | pro | enterprise | حزم الشراء (ريال) |
> |---|---:|---:|---:|---:|---|
> | واتساب المعاملات | 1000 | 3000 | 6000 | 12000 | 1000 بـ 79 · 3000 بـ 199 · 10000 بـ 599 |
> | واتساب التسويق | 150 | 400 | 900 | 1300 | 500 بـ 149 · 2000 بـ 499 · 5000 بـ 1099 |
> | البريد | 1500 | 4000 | 10000 | 25000 | 5000 بـ 39 · 20000 بـ 119 · 50000 بـ 249 |
> | الذكاء — نصوص | 300 | 800 | 2000 | 5000 | 100 بـ 49 · 300 بـ 129 · 1000 بـ 349 |
> | الذكاء — صور | 10 | 25 | 60 | 150 | 25 بـ 39 · 100 بـ 129 · 300 بـ 329 |
> | المجسمات الواقعية | 0 | 0 | 0 | 20 | 5 بـ 149 · 20 بـ 499 |
> | صور الطاولات | 10 | 20 | 40 | 80 | 30 بـ 49 · 100 بـ 139 |
> | مساعد الطلب للضيف | 500 | 1500 | 4000 | 10000 | 1000 بـ 49 · 5000 بـ 199 |
>
> السقوف القديمة لكل منشأة (`msgCapMonthly` و`aiLimits.monthly` و`ar3dMonthly`
> و`dinerAiMonthly`) **ما زالت تفوز** حيث ضبطها الكونسول، فلا يتغيّر حد منشأة مُعدّة يدوياً.
>
> **لم يُبنَ بعد:** المرحلة 2 (webhook واتساب — أكبر عائد على الإطلاق، ~6,420 دولار شهرياً
> عند 500 منشأة)، والمراحل 4-6 (Instagram، TikTok). زر X intent شُحن.
> الأدلة الأوّلية في `SPEND_SOCIAL_RESEARCH.md`.

---

# خطة حدود الإنفاق والنشر الاجتماعي — RBT360

تاريخ: 2026-07-28 · العملة: 1 USD = 3.75 SAR · كل الأرقام مُشتقّة من مصادر Meta / Google / X / TikTok الأوّلية المتحقَّق منها

---

## 1. الخلاصة التنفيذية

- **المنشأة الواحدة تكلّف المنصة اليوم نحو 72.50 دولار شهرياً (271.88 ﷼)** بملف استخدام متوسط، و**95.7% من هذا الرقم واتساب وحده** (19.26 دولار معاملات + 50.10 دولار تسويق). كل بنود Google وFirebase وResend مجتمعة أقل من 3.15 دولار.

- **الرياضيات الحالية خاسرة.** باقة `ops` تُباع بـ 199 ﷼ وتكلّف 271.88 ﷼ عند الاستخدام المتوسط. باقة `menu` بـ 99 ﷼. عند 500 منشأة بمتوسط `ops` تكون الإيرادات 99,500 ﷼ والتكلفة 135,937 ﷼ — **هامش سالب 37%**. الباقة الوحيدة الرابحة اليوم هي `enterprise`.

- **أكبر تعرّض غير محدود هو واتساب المعاملات.** `takeQuota()` في `functions/campaigns.js` يعدّ التسويق فقط؛ رسائل حالة الطلب في `messaging.js` ورابط الإيصال في `invoicing.js` والبريد عبر Resend كانت **بلا أي عدّاد ولا سقف ولا مفتاح إيقاف** — على بيانات اعتماد Meta الخاصة بالمنصة. حلقة trigger واحدة معطوبة تنفق بلا حد.

- **السقوف الحالية في `functions/spend.js` حواجز أمان لا حدود تجارية.** عند الاستهلاك الكامل: `menu` تكلّف 129 ﷼ (السعر 99)، `ops` تكلّف 457 ﷼ (السعر 199)، `pro` تكلّف 1,243 ﷼ (السعر 349)، `enterprise` تكلّف 3,331 ﷼ (السعر 549). **كل باقة خاسرة عند سقفها.** القسم 3 يعيد ضبطها إلى 31-36% من سعر الباقة.

- **أكبر رافعة توفير على الإطلاق ليست سقفاً بل تغيير مسار: نافذة الـ 24 ساعة.** قوالب Utility داخل نافذة خدمة عميل مفتوحة **مجانية**، والرسائل غير القالبية داخلها مجانية دائماً. رفع نسبة الرسائل التي تقع داخل النافذة من 40% إلى 80% يخفض بند المعاملات من 19.26 إلى 6.42 دولار للمنشأة — **توفير 6,420 دولار شهرياً (24,075 ﷼) عند 500 منشأة**. هذا يتطلب بناء webhook لـ Meta، وهو غير موجود اليوم.

- **جدول أسعار الوحدة داخل الكود خاطئ ويجب تصحيحه فوراً.** `UNIT_COST_USD` في `spend.js` يحمل `waUtility: 0.0157` و`waMarketing: 0.0384`. الصحيح من نقطة Meta الحيّة للسوق السعودي: **0.0107 و0.0501**. التسويق مُسعَّر بأقل من الحقيقة بـ 30%، أي أن قاطع الميزانية يتأخر عن الانطلاق.

- **النشر الاجتماعي: ابنِ Instagram فقط، وأجّل TikTok، ولا تبنِ X ولا Snapchat أبداً.** النشر عبر X API يكلّف 0.200 دولار للمنشور الحاوي رابطاً — 2,000 دولار شهرياً (7,500 ﷼) عند 500 منشأة × 20 منشوراً — مقابل صفر إيراد إضافي، إضافة إلى بند §III.L الذي يمنع صراحةً استخدام PPU لخدمة عدد غير محدود من المستخدمين النهائيين. البديل `x.com/intent/tweet` بتكلفة صفر وبلا مسؤولية قانونية. Snapchat بوابته allowlist عبر مندوب مبيعات، بلا نموذج تقديم أصلاً.

- **الحاجز الحقيقي أمام Instagram وTikTok ليس الكود بل المالك.** كلاهما يشترط App Review + Business Verification (سجل تجاري + شهادة ضريبة)، ولا SLA منشوراً لدى Meta. لا تُوعد أي منشأة بتاريخ إطلاق.

- **الحماية الفعلية ليست قاطع الميزانية بل الجدران الثلاثة لكل منشأة.** الـ rollup يعمل كل ساعتين؛ في أسوأ سيناريو تسمح السقوف اليومية بـ 250,000 رسالة معاملات يومياً عبر 500 منشأة = **2,675 دولار (10,031 ﷼) في اليوم**. الجدار الدقيقي (burst) هو ما يمنع الحلقة، والسقف الشهري هو ما يجعل سعر الباقة يغطي تكلفتها.

---

## 2. نموذج التكلفة

### 2.1 ملف الاستخدام المرجعي (منشأة متوسطة / شهر)

| القناة | الحجم | الافتراض |
|---|---:|---|
| واتساب معاملات — مُرسَل | 3,000 | 50 طلباً/يوم × 2 تحديث حالة |
| واتساب معاملات — مفوتر | 1,800 | 40% فقط تقع داخل نافذة 24 ساعة اليوم |
| واتساب تسويق | 1,000 | حملتان شهرياً × 500 مستلم |
| بريد Resend | 300 | فواتير + إيصالات + دعوات موظفين |
| Gemini نصوص | 800 استدعاء | مساعد المدير + مساعد الديّن (2,500 in / 400 out على `gemini-2.5-flash`) |
| Gemini صور | 25 صورة | `gemini-2.5-flash-image` |

### 2.2 أسعار الوحدة المعتمدة (مؤكدة من مصادر أوّلية)

| القناة | USD/وحدة | SAR/وحدة | المصدر |
|---|---:|---:|---|
| `waUtility` | 0.0107 | 0.0401 | نقطة تسعير Meta الحيّة، market=SA |
| `waMarketing` | 0.0501 | 0.1877 | نقطة تسعير Meta الحيّة، بلا شرائح حجم في السعودية |
| `email` | 0.0004 | 0.0015 | Resend Pro 20$ / 50,000 |
| `aiText` | 0.0018 | 0.0068 | `gemini-2.5-flash`: 0.30$/1M in + 2.50$/1M out |
| `aiImage` | 0.0390 | 0.1463 | `gemini-2.5-flash-image`، 1290 توكن للصورة 1024×1024 |

### 2.3 التكلفة لكل منشأة شهرياً

| البند | 10 منشآت | 100 منشأة | 500 منشأة |
|---|---:|---:|---:|
| واتساب معاملات (1,800 مفوترة) | 19.26 $ / 72.23 ﷼ | 19.26 $ / 72.23 ﷼ | 19.26 $ / 72.23 ﷼ |
| واتساب تسويق (1,000) | 50.10 $ / 187.88 ﷼ | 50.10 $ / 187.88 ﷼ | 50.10 $ / 187.88 ﷼ |
| بريد (300) | 0.00 $ / 0.00 ﷼ | 0.20 $ / 0.75 ﷼ | 0.32 $ / 1.20 ﷼ |
| Gemini نصوص (800) | 1.44 $ / 5.40 ﷼ | 1.44 $ / 5.40 ﷼ | 1.44 $ / 5.40 ﷼ |
| Gemini صور (25) | 0.98 $ / 3.66 ﷼ | 0.98 $ / 3.66 ﷼ | 0.98 $ / 3.66 ﷼ |
| بنية Firebase | 0.00 $ / 0.00 ﷼ | 0.31 $ / 1.15 ﷼ | 0.40 $ / 1.50 ﷼ |
| **الإجمالي / منشأة** | **71.78 $ / 269.18 ﷼** | **72.29 $ / 271.09 ﷼** | **72.50 $ / 271.88 ﷼** |
| **الإجمالي / المنصة** | **717.80 $ / 2,691.75 ﷼** | **7,229.00 $ / 27,108.75 ﷼** | **36,250.00 $ / 135,937.50 ﷼** |

**تفصيل بند البنية (المنصة/شهر):**

| البند | 10 | 100 | 500 |
|---|---:|---:|---:|
| Firestore (سكيدولر 15 دقيقة + قراءات التطبيق) | 0 $ | 25.56 $ | 155.00 $ |
| Cloud Functions (2M استدعاء + 400k GB-s مجاناً) | 0 $ | 0 $ | 18.00 $ |
| Cloud Scheduler (3 وظائف مجاناً/حساب فوترة) | 0 $ | 0 $ | 0 $ |
| Hosting + Storage | 0 $ | 5.00 $ | 27.00 $ |
| Resend | 0 $ (Free 3,000) | 20.00 $ (Pro) | 160.00 $ (Scale 200k) |
| **مجموع البنية** | **0 $** | **50.56 $** | **360.00 $** |

### 2.4 المقارنة بالإيراد — لماذا هذه الخطة موجودة

| المتوسط المفترض للباقة | الإيراد/500 منشأة | التكلفة/500 منشأة | الهامش |
|---|---:|---:|---:|
| `menu` 99 ﷼ | 49,500 ﷼ | 135,937 ﷼ | **-175%** |
| `ops` 199 ﷼ | 99,500 ﷼ | 135,937 ﷼ | **-37%** |
| `pro` 349 ﷼ | 174,500 ﷼ | 135,937 ﷼ | +22% |
| `enterprise` 549 ﷼ | 274,500 ﷼ | 135,937 ﷼ | +50% |

### 2.5 أثر رافعة نافذة الـ 24 ساعة (بعد بناء الـ webhook)

| نسبة الرسائل داخل النافذة | مفوترة/منشأة | تكلفة المعاملات/منشأة | التوفير عند 500 منشأة |
|---:|---:|---:|---:|
| 40% (اليوم) | 1,800 | 19.26 $ / 72.23 ﷼ | — |
| 60% | 1,200 | 12.84 $ / 48.15 ﷼ | 3,210 $ / 12,038 ﷼ شهرياً |
| 80% (المستهدف) | 600 | 6.42 $ / 24.08 ﷼ | 6,420 $ / 24,075 ﷼ شهرياً |

---

## 3. حدود الإنفاق المقترحة لكل باقة

الباقات القائمة (من `src/lib/plans.js` و`PLAN_PRICES` في `functions/platformExtensions.js`): `menu` 99 ﷼ · `ops` 199 ﷼ · `pro` 349 ﷼ · `enterprise` 549 ﷼.

**قاعدة الضبط:** تكلفة القنوات عند الاستهلاك الكامل ≤ **35% من سعر الباقة**، بحساب واتساب المعاملات **بالسعر الكامل** (0.0107) لا بالسعر المخفَّض داخل النافذة — أي أسوأ حالة. الـ 65% الباقية تغطي البنية والدعم والهامش.

### 3.1 السقوف الشهرية المقترحة

| القناة | menu (99 ﷼) | ops (199 ﷼) | pro (349 ﷼) | enterprise (549 ﷼) |
|---|---:|---:|---:|---:|
| `waUtility` | 400 | 1,000 | 2,000 | 3,000 |
| `waMarketing` | 60 | 100 | 120 | 120 |
| `email` | 500 | 1,500 | 4,000 | 8,000 |
| `aiText` | 300 | 600 | 1,200 | 2,000 |
| `aiImage` | 8 | 20 | 40 | 80 |
| **التكلفة عند الاستهلاك الكامل** | **8.34 $ / 31.28 ﷼** | **18.17 $ / 68.14 ﷼** | **32.73 $ / 122.74 ﷼** | **48.03 $ / 180.11 ﷼** |
| **النسبة من سعر الباقة** | **31.6%** | **34.2%** | **35.2%** | **32.8%** |

قارنها بالسقوف الموجودة اليوم في `PLAN_QUOTAS` داخل `functions/spend.js`:

| الباقة | التكلفة عند السقف الحالي | السعر | الفجوة |
|---|---:|---:|---:|
| menu (2000/200/3000/500/20) | 129.00 ﷼ | 99 ﷼ | **-30%** |
| ops (6000/1000/8000/1500/40) | 457.73 ﷼ | 199 ﷼ | **-130%** |
| pro (15000/3000/20000/4000/120) | 1,243.05 ﷼ | 349 ﷼ | **-256%** |
| enterprise (40000/8000/50000/12000/400) | 3,331.50 ﷼ | 549 ﷼ | **-507%** |

### 3.2 الجدران اليومية واللحظية

تبقى المعادلة القائمة في `spend.js`: `day = max(ceil(month / 6), min(month, 50))`، والجدار الدقيقي ثابت لكل قناة لا لكل باقة.

| القناة | السقف اليومي المشتق (menu → enterprise) | الجدار الدقيقي |
|---|---|---:|
| `waUtility` | 67 / 167 / 334 / 500 | 20/دقيقة (خُفِّض من 40) |
| `waMarketing` | لا يوجد — السقف الشهري هو الجدار (BULK) | 0 |
| `email` | 84 / 250 / 667 / 1,334 | 80/دقيقة |
| `aiText` | 50 / 100 / 200 / 334 | 30/دقيقة |
| `aiImage` | 50 / 50 / 50 / 50 → اضبط الأرضية إلى 10 لهذه القناة | 4/دقيقة (خُفِّض من 8) |

`DAY_FLOOR = 50` مناسب للرسائل لكنه كارثي للصور: منشأة `menu` سقفها 8 صور/شهر ستحصل على سقف يومي 8، وهذا صحيح، لكن `pro` سقفها 40 ستحصل على 50 — أي شهرها كله في يوم. **أضف أرضية خاصة بالقناة: `DAY_FLOOR_BY_CHANNEL = { aiImage: 10 }`.**

### 3.3 حزم التجاوز (بدل الرفض عند الحاجة الحقيقية)

المنشأة التي تتجاوز سقفها لا تُترك عاجزة — تشتري، على غرار حزم الذكاء القائمة `{100: 49, 300: 129, 1000: 349}`:

| الحزمة | الكمية | السعر | التكلفة | الهامش |
|---|---:|---:|---:|---:|
| رسائل معاملات | 1,000 | 69 ﷼ | 40.10 ﷼ | 42% |
| رسائل معاملات | 3,000 | 179 ﷼ | 120.30 ﷼ | 33% |
| رسائل تسويقية | 500 | 149 ﷼ | 93.85 ﷼ | 37% |
| رسائل تسويقية | 1,000 | 279 ﷼ | 187.70 ﷼ | 33% |
| صور AI | 50 | 49 ﷼ | 7.31 ﷼ | 85% |

تُخزَّن في `tenant.spendExtra.{channel}` وتُضاف إلى السقف الشهري في `limitsFor()` بنفس آلية `tenant.aiExtra` الموجودة لـ `aiText`.

### 3.4 ميزانية المنصة

| عدد المنشآت | الإنفاق المتوقع | `monthlyBudgetUsd` | `budgetAction` |
|---:|---:|---:|---|
| 10 | 718 $ | 1,000 | `warn` |
| 100 | 7,229 $ | 9,000 | `stopMarketing` |
| 500 | 36,250 $ | 45,000 | `stopMarketing` |

`stopAll` لا يُستخدم إلا يدوياً: إيقاف واتساب المعاملات يعني ديّناً لا يعلم أن طلبه جاهز.

---

## 4. المعمارية

### 4.1 مسارات Firestore

| المسار | الغرض | الصلاحيات |
|---|---|---|
| `tenants/{tid}/counters/spend-YYYY-MM` | عدّاد الشهر/اليوم/الدقيقة لكل قناة | قراءة: مدير المنشأة · كتابة: **مرفوضة للعميل تماماً** (Admin SDK فقط) |
| `platformConfig/spendControls` | مفتاح الإيقاف + أسعار الوحدة + الميزانية | super-admin فقط |
| `platformStats/spend-YYYY-MM` | التجميع عبر المنشآت (يُعاد بناؤه، لا يُزاد) | قراءة super-admin |
| `platformActivity` | أحداث `spendLimit` و`spendBudget` | قراءة super-admin |
| `tenants/{tid}/waWindows/{phoneHash}` | **جديد** — `lastInboundAt` لحساب نافذة الـ 24 ساعة | Admin SDK فقط |
| `tenants/{tid}/waLedger/{wamid}` | **جديد** — إيصالات Meta: `billable`, `pricing_model`, `category` للمطابقة المحاسبية | Admin SDK فقط |

### 4.2 شكل مستند العدّاد

```
tenants/{tid}/counters/spend-2026-07
{
  period: '2026-07',
  m: { waUtility: 812, waUtilityFree: 1190, waMarketing: 400, email: 96, aiText: 340, aiImage: 11 },
  d: { day: '2026-07-28', waUtility: 41, email: 6, aiText: 22 },
  b: { minute: '2026-07-28, 14:07', waUtility: 3 },
  blocked:       { waMarketing: 120 },
  blockedReason: { waMarketing: 'cap' },
  flagged:       { waMarketing: true },
  createdAt, updatedAt
}
```

معاملة واحدة على مستند واحد تفحص الجدران الثلاثة معاً وتعيد `granted` — المُرسِل يُرسل هذا العدد بالضبط ولا وحدة زيادة. الخريطة `d` و`b` **تُستبدل كاملةً** عند تغيّر اليوم أو الدقيقة (لا تُدمج بمسار منقّط) وإلا حُسبت قنوات الأمس على اليوم.

### 4.3 مواضع القياس — الملفات والدوال بالاسم

| الملف · الدالة | القناة | التعديل |
|---|---|---|
| `functions/spend.js · takeSpend(db, tid, channel, want, {tenant})` | — | المحرّك. صحّح `UNIT_COST_USD` إلى `waUtility: 0.0107`, `waMarketing: 0.0501`, `aiText: 0.0018`. أعد ضبط `PLAN_QUOTAS` وفق جدول 3.1. أضف `waUtilityFree` إلى `CHANNELS` بسقف `-1` (يُعدّ ولا يُرفض). |
| `functions/messaging.js · claim(meter, kind)` | البوابة الوحيدة | بوابة واحدة يمر بها `sendEmail` و`sendWhatsAppTemplate` و`sendWhatsAppText`. كل مُنادٍ يمرّر `{db, tid, channel, tenant}` أو الحرف `'platform'` كإفصاح صريح عن الاستثناء. أي نداء بلا `meter` يسجّل `[spend] UNMETERED`. |
| `functions/messaging.js` — العشرة triggers | `waUtility` / `email` | تمرير `meter` من مستند المنشأة الذي بحوزة الـ trigger أصلاً (صفر قراءة إضافية). |
| `functions/messaging.js` — دعوة موظف | `email` | تُقاس على المنشأة الداعية لا على المنصة — عاصفة دعوات مبرمَجة هي الإساءة المقصودة بالحد. |
| `functions/invoicing.js` — رابط الإيصال + `sendEmail` | `waUtility` / `email` | تمرير `meter: {db, tid, channel}`. |
| `functions/campaigns.js · takeQuota(db, tid, want)` | `waMarketing` | يفوّض إلى `takeSpend(db, tid, 'waMarketing', want)` بمطالبة **مجمّعة** واحدة قبل التوزيع. `BULK_CHANNELS` تُلغي الجداران اليومي والدقيقي لهذه القناة وإلا قُصّت حملة 2,000 مستلم إلى 400. |
| `functions/index.js` — `geminiProxy` (~س 425) | `aiText` / `aiImage` | جدار 30/دقيقة القائم يبقى كحائط أول (يوفّر معاملة)، ثم `takeSpend` بقناة مشتقة من `/-image$/.test(model)`. |
| `functions/platformExtensions.js · generateTableImage` (س 1122) | `aiImage` | يستبدل عدّاده الخاص (30/شهر) بـ `takeSpend`. |
| `functions/platformExtensions.js` — تحويل AR 3D (س 983) | `ar3d` (قناة جديدة) | يبقى العداد لكن يُهاجَر إلى نفس المستند ليظهر في الكونسول ولوحة الإنفاق. |
| `functions/platformExtensions.js` — مساعد الديّن (س 1216) | `dinerAi` (قناة جديدة) | حدّ 2000/شهر + 20/دقيقة يُهاجَر إلى `takeSpend`. |
| `functions/index.js · exports.spendRollup` (س 1012) | — | مجدولة كل ساعتين، تعيد بناء `platformStats/spend-YYYY-MM` بتزامن محدود 25، وتنفّذ قاطع الميزانية. |

### 4.4 مفتاح إيقاف المنصة

```
platformConfig/spendControls
{
  killAll: false,
  channels: { waMarketing: false, waUtility: false, email: false, aiText: false, aiImage: false },
  unitCostUsd: { waUtility: 0.0107, waMarketing: 0.0501, email: 0.0004, aiText: 0.0018, aiImage: 0.039 },
  monthlyBudgetUsd: 9000,
  budgetAction: 'warn' | 'stopMarketing' | 'stopAll',
  budgetFired: { period: '2026-07', warn: false, trip: false, at }
}
```

مُخزَّن في ذاكرة النسخة لمدة 60 ثانية — نسخة ساخنة ترسل حملة يجب ألا تقرأه لكل رسالة، ودقيقة تأخير مقبولة على إيقاف طارئ. `invalidateControls()` تُستدعى بعد كتابة الكونسول. **مستند غير مقروء أو غير موجود = لا شيء موقوف** — فشل قراءة يجب ألا يتنكّر في هيئة «المالك ضغط إيقاف».

قاطع الميزانية داخل الـ rollup: عند 80% تنبيه، وعند 100% ينفّذ `budgetAction` ويكتب `budgetFired` مرة واحدة في الشهر لكل عتبة حتى لا يُنبّه كل ساعتين.

### 4.5 سلوك التجاوز — إيقاف صارم أم تدهور

| القناة | السلوك | السبب |
|---|---|---|
| `waMarketing` | **إيقاف صارم** | تسويق، لا التزام تجاه الديّن. المطالبة المجمّعة تعني أن الحملة تُرفض قبل بدئها لا في منتصفها. |
| `aiImage` | **إيقاف صارم** + عرض شراء حزمة | أغلى وحدة في المنظومة (0.039 $) وقيمتها اختيارية. |
| `aiText` | **إيقاف صارم** + رسالة عربية تُفرِّق بين `cap` و`daily` و`burst` و`killed` (مُنفَّذ في `index.js`) | له حزم رصيد مُسعَّرة أصلاً. |
| `waUtility` | **تدهور متدرّج**: (1) داخل نافذة 24 ساعة → رسالة نصية حرة مجانية بدل القالب؛ (2) خارجها وبلا رصيد → إشعار داخل التطبيق + push؛ (3) لا يُسقَط الحدث صامتاً أبداً | إسكات إشعار «طلبك جاهز» ضرر تشغيلي على الديّن، لا توفير. |
| `email` | **تدهور**: إشعار داخل التطبيق، والفاتورة تبقى قابلة للتنزيل من `/invoice/:tid/:id` | البريد أرخص بند (0.0004 $) — الإيقاف الصارم فيه لا يوفّر شيئاً. |
| أي خطأ بنية تحتية | **fail-open** بـ `reason: 'error-open'` | انقطاع Firestore عابر يجب ألا يُسكِت إشعار ديّن. **قيّد ذلك:** عدّاد في ذاكرة النسخة يسمح بـ 200 منح `error-open` كحد أقصى قبل أن يتحوّل إلى fail-closed — وإلا صار انقطاع Firestore إنفاقاً غير محدود. |

قاعدة عامة: **fail-open على البنية، fail-closed على السياسة.** كل رفض يُعدّ في `blocked.{channel}` مع `blockedReason` ليعرف المشغّل أي جدار ضُرب: `cap` تعني ارفع الباقة، `burst` تعني ابحث عن الحلقة.

### 4.6 ظهور الأمر في الكونسول

- **`src/routes/platform/Spend.jsx`** — لوحة إنفاق المنصة: إجمالي الشهر بالدولار والريال، نسبة الميزانية، جدول المنشآت مرتباً بالتكلفة، أعمدة `blocked_*`، ومفاتيح الإيقاف لكل قناة + `killAll`. قراءة **مستند واحد** (`platformStats/spend-YYYY-MM`) لا مسح عبر المنشآت.
- **`src/routes/platform/VenueDetail.jsx`** — تفصيل المنشأة: الاستخدام مقابل السقف لكل قناة، حقول تعديل `spendCaps` و`spendCapsDaily`، وزر منح حزمة تجاوز.
- **`src/components/SpendMeter.jsx`** — عدّاد يراه صاحب المنشأة نفسه: شريط لكل قناة، حالة `is-near` عند 80% و`is-full` عند 100%، وزر شراء حزمة عند الامتلاء. الشفافية هنا تقلّل تذاكر الدعم وتبيع الحزم.
- **`platformActivity`** — أول رفض في الشهر لكل قناة يكتب حدثاً واحداً (`flagged` يمنع التكرار) في نفس التغذية التي يفتحها المشغّل أصلاً، بشدة `high` لـ `burst` و`warn` لغيره.
- **قواعد Firestore** — يجب نشرها مع هذه الحزمة:
  ```
  match /tenants/{tid}/counters/{doc} { allow read: if isManager(tid); allow write: if false; }
  match /platformConfig/{doc}         { allow read, write: if isSuperAdmin(); }
  match /platformStats/{doc}          { allow read: if isSuperAdmin(); allow write: if false; }
  ```
  بدون هذه القواعد يستطيع موظف رفع سقف منشأته بكتابة مباشرة — وهو بالضبط الخلل الذي كان في حدود المساعد قبل نقلها للخادم.

### 4.7 المكوّن الناقص الأهم — webhook واتساب

لا يوجد اليوم أي نقطة استقبال من Meta، ما يعني: لا إيصالات تسليم، لا مطابقة فوترة، ولا معرفة بنافذة الـ 24 ساعة.

```
functions/waWebhook.js
  GET  /waWebhook   → تحقق hub.challenge (verify token في السر)
  POST /waWebhook   → توقيع X-Hub-Signature-256 إلزامي، ثم:
     messages[]  → tenants/{tid}/waWindows/{sha256(phone)} = { lastInboundAt, expiresAt: +24h }
     statuses[]  → tenants/{tid}/waLedger/{wamid} = { status, pricing.billable,
                                                       pricing.pricing_model, pricing.category }
```

ثم في `messaging.js` قبل أي رسالة معاملات:

```
windowOpen = waWindows/{hash}.expiresAt > now
if (windowOpen) → sendWhatsAppText(...)      // حرة، مجانية، تُعدّ في waUtilityFree
else            → sendWhatsAppTemplate(...)  // مفوترة، تُعدّ في waUtility
```

`waLedger` يعطي بعد شهر واحد الرقم الحقيقي لنسبة `billable: false`، فتُستبدل تقديرات هذا المستند بأرقام مقيسة، وتُطابَق فاتورة Meta بند بند.

---

## 5. النشر الاجتماعي — القرار لكل منصة

| المنصة | القرار | السبب في سطر |
|---|---|---|
| **Instagram** | **BUILD** | الـ API موجود ومكتمل وبلا رسوم منشورة؛ الكلفة الوحيدة زمن التطوير + App Review و Business Verification. |
| **X (Twitter)** | **NEVER** (API) — **BUILD** رابط intent | 0.200 $ للمنشور الحاوي رابطاً = 7,500 ﷼ شهرياً عند 500 منشأة مقابل صفر إيراد، و§III.L يحصر PPU في «عدد محدود من المستخدمين النهائيين»، وحظر المحتوى المتشابه عبر الحسابات خطر إيقاف. |
| **TikTok** | **DEFER** إلى المرحلة 5 | الـ API موجود لكن العميل غير المُدقَّق محصور في **5 مستخدمين/24 ساعة** وكل منشور `SELF_ONLY` — لا يمكن تسليمه لأي منشأة قبل اجتياز تدقيقين متتاليين. |
| **Snapchat** | **NEVER** | البوابة `allowlist only` عبر مندوب Snap، **لا يوجد نموذج تقديم عام أصلاً**. البديل: تصدير الأصل بالمقاس (mp4، 5-60 ثانية، ≥540×960) للنشر اليدوي. |

### 5.1 Instagram — المعمارية الدنيا

**اختر مسار Facebook Login for Business لا Instagram Login.** السبب واحد وحاسم: توكن الصفحة في مسار Facebook **لا ينتهي**، بينما مسار Instagram يفرض دورة تجديد كل 60 يوماً على مئات المنشآت — وأي منشأة يفشل تجديدها تسقط صامتة. الثمن: اشتراط صفحة فيسبوك مرتبطة أثناء الإعداد، وهو احتكاك مرة واحدة مقابل عبء تشغيلي دائم.

```
tenants/{tid}/private/social/instagram      (Admin SDK فقط — العميل لا يقرأه إطلاقاً)
{ igUserId, pageId, token: <KMS-encrypted>, tokenType: 'page',
  scopes: ['instagram_basic','instagram_content_publish','pages_read_engagement'],
  status: 'connected' | 'needs_reconnect', connectedBy, connectedAt }

tenants/{tid}/socialPosts/{postId}
{ text, mediaUrl, normalizedUrl, targets: ['instagram'], scheduledAt,
  state: 'draft'|'queued'|'creating'|'publishing'|'published'|'failed',
  containerId, igMediaId, attempts, lastError, publishedAt }
```

دوال:

| الدالة | النوع | المهمة |
|---|---|---|
| `socialOAuthStart` | callable | تُصدر رابط تفويض + `state` موقّع مرتبط بـ tid |
| `socialOAuthCallback` | HTTPS | تبادل الكود، جلب توكن الصفحة عبر `?accounts`، تشفير وتخزين |
| `socialNormalizeMedia` | داخلية | **إلزامية**: JPEG + sRGB + قص إلى 4:5–1.91:1 + عرض ≤ 1440 + حجم ≤ 8 MB + رفع إلى رابط HTTPS عام غير محمي (خوادم Meta هي التي تجلبه). أي PNG/WebP/HEIC من صاحب المقهى يفشل بدونها. |
| `socialPublishTick` | مجدولة كل 5 دقائق | تلتقط المستحق، تنشئ الحاوية **قرب وقت النشر** (تموت بعد 24 ساعة)، تستطلع `status_code` بتراجع أسّي 5s/15s/45s/2m، ثم `media_publish` |

انضباط الحدود: اقرأ `GET /{ig-id}/content_publishing_limit` قبل كل نشر، **وهندس على 50 منشوراً/24 ساعة لا 100** (وثائق Meta متناقضة والمرجع يقول 50). الحد القاتل الحقيقي هو `4800 × الانطباعات` — حساب مقهى جديد ميزانيته شبه صفرية. عالج الخطأ **80002** والخطأ **17** كحالتين منفصلتين. **ثبّت `v25.0`**، ورابط رفع الفيديو يجب أن يحوي مقطع الإصدار: `rupload.facebook.com/ig-api-upload/v25.0/{container_id}`.

لا تَعِد بـ Stories: الوثائق تحصرها في حسابات **Business** لا Creator. لا جدولة من Meta — أنت من يبنيها.

### 5.2 X — رابط النية

من داخل Post Studio، زر واحد يفتح:

```
https://x.com/intent/tweet?text=<encoded>&url=<venue-menu-link>
```

بلا تطبيق، بلا مفاتيح، بلا OAuth، بتكلفة صفر، وبصفر مسؤولية تحت اتفاقية مطوّري X. RBT360 يحتفظ بالقيمة الحقيقية — توليد النص والصورة — ويسقط 100% من الكلفة والمسؤولية. هذه هي المعمارية الصحيحة لا خطة الطوارئ. **إن أُصرّ لاحقاً على API: امنع الروابط داخل نص المنشور خادمياً** (0.200 $ مقابل 0.015 $ = فرق 13.3 ضعفاً)، وضع الرابط في الـ bio.

### 5.3 TikTok — عند البناء (المرحلة 5)

ابدأ بـ **`video.upload`** (مسودة الوارد) لا `video.publish`: المنشئ يكمل النشر داخل تطبيق TikTok، فتشحن قبل التدقيق وتتجنّب قيد `SELF_ONLY`. شاشة النشر عمل حقيقي لا زر: nickname حيّ من `creator_info`، قائمة خصوصية **بلا قيمة افتراضية**، ثلاثة مفاتيح (Comments/Duet/Stitch) **معطّلة افتراضياً** ورمادية إن كانت معطّلة في الحساب، مفتاح محتوى تجاري، ونص «By posting, you agree to TikTok's Music Usage Confirmation» حرفياً. إسقاط أي عنصر = رسوب التدقيق. `upload_url` صالح **ساعة واحدة فقط** — لا تُصدره ثم تصفّه في طابور طويل. 6 طلبات/دقيقة لكل توكن. لا تحسب السقف اليومي محلياً — التقط `spam_risk_too_many_posts` و`reached_active_user_cap`.

---

## 6. خطة التنفيذ

| المرحلة | المحتوى | الجهد | يحجبها |
|---|---|---:|---|
| **0. تثبيت المقياس القائم** | `functions/spend.js` + `spend.test.js` موجودان في شجرة العمل غير مُلتزَمَين، والقياس موصول في `messaging.js` و`campaigns.js` و`index.js`. التزم، انشر قواعد Firestore (4.6)، وصحّح `UNIT_COST_USD` إلى 0.0107 / 0.0501 / 0.0018. | **1 يوم** | — |
| **1. الضبط التجاري** | استبدل `PLAN_QUOTAS` بجدول 3.1، أضف `DAY_FLOOR_BY_CHANNEL.aiImage = 10`، اخفض burst `waUtility` إلى 20 و`aiImage` إلى 4، أضف `tenant.spendExtra` وحزم التجاوز في `createPayIntent`، اضبط `monthlyBudgetUsd` و`budgetAction`، وحدّ منح `error-open` بـ 200/نسخة. | **3 أيام** | — |
| **2. webhook واتساب — أكبر عائد** | `waWebhook` (تحقق + توقيع)، `waWindows`، `waLedger`، وتوجيه رسالة حرة بدل القالب داخل النافذة، وقناة `waUtilityFree`. عائد متوقع 6,420 $/شهر عند 500 منشأة. | **5 أيام** | تسجيل webhook في تطبيق Meta |
| **3. هجرة العدّادات المتفرقة** | `generateTableImage` و`ar3dMonthly` و`dinerAiMonthly` إلى `takeSpend` بقنوات `aiImage`/`ar3d`/`dinerAi` — مقياس واحد ولوحة واحدة. | **2 يوم** | — |
| **4. X — رابط النية** | زر مشاركة في Post Studio. | **1 يوم** | — |
| **5. Instagram** | OAuth (مسار Facebook Login) + تشفير التوكن + تطبيع الوسائط (sharp) + طابور النشر + استطلاع بتراجع أسّي + شاشة الربط في `/admin`. | **12-16 يوم عمل** | **App Review + Business Verification — لا SLA منشور** |
| **6. TikTok** | `video.upload` أولاً + شاشة الامتثال الكاملة + التقاط أخطاء السقف. | **10-14 يوم عمل** | مراجعة التطبيق (عدة أيام إلى أسبوعين) ثم تدقيق النشر |

**ترتيب إلزامي:** 0 → 1 → 2 قبل أي شيء اجتماعي. المرحلة 2 وحدها توفّر أكثر مما تكلّفه المراحل 5 و6 مجتمعتين.

**مسار متوازٍ يبدأ اليوم:** قدّم Business Verification لدى Meta فوراً، وابنِ نداء API ناجحاً واحداً على الأقل قبل التقديم (شرط صريح من Meta).

---

## 7. المخاطر

### 7.1 ما يمكن أن ينكسر داخلياً

| الخطر | الأثر | الإجراء |
|---|---|---|
| `fail-open` على خطأ Firestore | انقطاع Firestore = إنفاق غير محدود، والمقياس نفسه هو ما يفشل | حدّ 200 منح `error-open` لكل نسخة ثم تحوّل إلى fail-closed |
| الـ rollup كل ساعتين | نافذة عمياء مدتها ساعتان أمام أي جموح | الحماية الحقيقية هي الجداران اليومي والدقيقي لكل منشأة، لا قاطع الميزانية. أسوأ سقف يومي عبر 500 منشأة = 250,000 رسالة = **2,675 $ / 10,031 ﷼ في اليوم** |
| `PLAN_QUOTAS` مكرّر يدوياً من `src/lib/plans.js` | تعديل باقة في جانب واحد يفتح ثغرة صامتة | تعليق `change BOTH sides together` قائم — أضف اختباراً في `spend.test.js` يقارن قائمة الـ id في الملفين |
| `msgCapMonthly` القديم لا يزال حاكماً لـ `waMarketing` | منشأة ضُبطت يدوياً في الكونسول تتجاوز سقف باقتها الجديد | مقصود ومطلوب للتوافق — لكن أظهر «سقف مخصّص» بوضوح في `VenueDetail.jsx` |
| `DAY_FLOOR = 50` على `aiImage` | باقة `pro` تحرق شهرها (40 صورة) في يوم | `DAY_FLOOR_BY_CHANNEL` في المرحلة 1 |

### 7.2 ما يمكن أن تغيّره Meta

- **تحديث الأسعار كل ربع** (1 يناير / أبريل / يوليو / أكتوبر) بإشعار شهر واحد. السعودية تغيّرت مرتين مؤكدتين: Utility انخفض 2025-10-01، Marketing ارتفع 2026-04-01. **لا تثبّت الأسعار في الكود** — `unitCostUsd` في `spendControls` قابلة للتحرير، ومهمة ربع سنوية تعيد الاستعلام. النافذة القادمة 2026-10-01.
- **إعادة تصنيف القوالب بإشعار يوم واحد.** جملة تسويقية واحدة داخل قالب حالة طلب تنقله من Utility إلى Marketing: **من 0.0107 إلى 0.0501 — زيادة 4.7 أضعاف** وفقدان المجانية داخل النافذة. افصل قوالب المعاملات عن أي ترويج فصلاً تاماً، والتقط `REJECTED / INCORRECT_CATEGORY`.
- **حدود المراسلة على مستوى المحفظة لا الرقم** (250 → 2,000 → 10,000 → 100,000 → Unlimited). نموذج OBO **ملغى**، وWABA تنتمي لمحفظة واحدة، والحد الأولي رقمان (حتى 20). **لا تبنِ على رقم واتساب واحد مشترك** — منشأة واحدة تُسيء الإرسال تخنق الجميع. الحل: WABA لكل منشأة عبر Embedded Signup.
- **قرار نموذج الفوترة نهائي عملياً.** Solution Partner = أنت «Bill To Party» ومسؤول أمام Meta عن إنفاق كل منشأة. Tech Provider = صفر مسؤولية مالية مقابل احتكاك (بطاقة لكل منشأة بعد اكتمال التسجيل). **خط الائتمان لا يُغيَّر بعد ربطه بـ WABA — يلزم إنشاء WABA جديدة.** اتخذ القرار قبل أول تسجيل.
- **Instagram: الحاوية تموت بعد 24 ساعة**، والحد الحقيقي `4800 × الانطباعات` لا الرقم المعلن، والوثائق نفسها متناقضة (50 مقابل 100). لا يوجد أي معامل جدولة موثّق رغم صفحات Meta التسويقية.

### 7.3 ما يمكن أن تغيّره X

- **§III.L مُلزِمة:** PPU مخصّصة لتطبيقات «بعدد محدود من المستخدمين النهائيين»، ويجب طلب Enterprise عند التجاوز. أسعار Enterprise غير منشورة ومتداولة عند ~42,000 $/شهر بلا مصدر أوّلي.
- **حظر المحتوى المتشابه عبر الحسابات** خطر إيقاف لا مسألة ذوق.
- **سقف المسؤولية 50 دولاراً** — X لا تتحمّل شيئاً إن سقط شيء.
- كل ذلك ينتفي بالكامل مع مسار `intent` — وهذا هو السبب الجذري للتوصية.

### 7.4 ما يحتاج المالك شخصياً

| المطلوب | لأجل | ملاحظة |
|---|---|---|
| السجل التجاري + شهادة ضريبة القيمة المضافة | Meta Business Verification | الوثائق المقبولة للسعودية **غير موثّقة في مصدر أوّلي** — عاملها كتوقّع معقول لا كحقيقة |
| نداء API ناجح واحد على الأقل | شرط صريح لطلب Advanced Access | يجب إنجازه قبل التقديم |
| أيقونة 1024×1024 + رابط سياسة خصوصية + تصنيف + بريد عمل | إعدادات تطبيق Meta | سياسة الخصوصية موجودة في `/legal` |
| تسجيل شاشة بتعليقات إنجليزية | مراجعة Meta وTikTok — الواجهة عربية | «Use English as the app UI language» أو أضف captions/tooltips |
| اعتماد بيانات Meta Cloud API لكل منشأة (Embedded Signup) | فوترة واتساب لكل منشأة | القرار بين Solution Partner و Tech Provider مالي لا تقني |
| حساب مطوّر TikTok + روابط Privacy/ToS ظاهرة دون فتح قائمة | مراجعة TikTok | «Apps still in development or testing will not be approved» |
| أهلية الكيان السعودي لدى TikTok | — | **صامتة في المصادر الأوّلية** — لا سبيل للحسم سوى التسجيل الفعلي |
| فاتورة BSP الأولى | تثبيت السعرين U و M فعلياً | كل رقم واتساب في هذا المستند من نقطة Meta الحيّة، لا من فاتورة — استبدلها بعد أول دورة |

### 7.5 الافتراضات التي تنهار إن اختلفت المدخلات

- **نسبة 40% داخل نافذة الـ 24 ساعة تقدير غير مقيس.** إن كانت الحقيقة 10%، يقفز بند المعاملات من 19.26 إلى 28.89 دولار للمنشأة (+50%) وتصبح باقة `pro` هي الأخرى خاسرة. `waLedger` في المرحلة 2 يقيسها فعلياً خلال شهر.
- **1,000 رسالة تسويقية شهرياً افتراض سلوكي.** منشأة نشطة قد ترسل 4,000 — لذا `waMarketing` مُقيَّدة بـ 120 في جدول 3.1 وما فوقها يُشترى بحزمة بهامش 33%، لا يُمنح.
- **بند البنية عند 500 منشأة (360 $) مُستقرَأ لا مقيس.** المؤكد فقط: قراءة Firestore 0.03 $/100k، كتابة 0.09 $/100k، و2M استدعاء + 400k GB-s + 200k CPU-s مجاناً. أسعار me-central1/me-central2 لم تُستخرج — اسحبها من محدد المنطقة قبل أي قرار إقامة بيانات في السعودية.
- **الطبقة المجانية لـ Gemini ممنوعة في الإنتاج لسبب قانوني قبل أي سبب فني:** محتوى الطبقة المجانية يُستخدم لتحسين منتجات Google، والمدفوعة لا. بيانات طلبات العملاء تخرج من النقاش عند هذا الحد.
- **خفض تكلفة الذكاء متاح فوراً بلا مخاطرة:** `gemini-2.5-flash-lite` (0.10 / 0.40) يخفض بند النص إلى الثلث، و`Batch` على `gemini-2.5-flash-image` يخفض الصورة من 0.039 إلى **0.0195** — نصف السعر بلا تغيير موديل. **لا تنتقل إلى `gemini-3.1-flash-image` ظناً أنه أرخص: هو 0.0672 للصورة، أي أغلى 1.7 ضعف.**