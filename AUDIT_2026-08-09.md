# تقرير التدقيق الشامل — RBT360 — 2026-08-09

تدقيق عدائي كامل للمنصة بعد جولة تطوير ضخمة (65 ملفاً، +5367/−939 سطراً غير منشورة): جلسات PIN حقيقية، قبول حصري للطلبات، حالة طاولة مخزّنة، كتابات طلب متفائلة، إعادة تصميم الكاشير ونافذة الطلب، فصل الإشعارات، تخصيصات المنيو، مدخل `/app` وتطبيق موظفين لكل منشأة.

**المنهجية:** فحص آلي (lint / guard / build / require / اختبارات) → 4 وكلاء تدقيق متوازين لكل نظام فرعي (نجح منها تدقيقان مستقلان لجلسات PIN تحقّقا من بعضهما، وتدقيق الإشعارات؛ ودقّقتُ الطلبات/المال والطاولات يدوياً) → تحقّق عدائي لكل نتيجة (مؤكّد/مرفوض/محتمل) → إصلاح كل ما يمكن إصلاحه بأمان → إعادة تحقّق.

**النتيجة:** كل الإصلاحات مطبّقة، والبناء أخضر: `eslint` صفر أخطاء، `guard` نظيف (لا إيموجي ولا أرقام هندية)، كل دوال Firebase `require()` سليمة، ٣٣ حالة اختبار وحدة تنجح.

الحالة: **كل التعديلات في شجرة العمل غير منشورة** (كما كانت جولة التطوير). خطوات النشر في آخر التقرير.

---

## أعطال حيّة عولجت أثناء التدقيق (كان يختبرها المالك مباشرة)

### L-1 — [حرج/حيّ] الدخول بالرمز يعطي 500 (createCustomToken بلا صلاحية IAM)
`functions/staffSecurity.js:216` — دالة `pinSignIn` تصل لخطوة `getAuth().createCustomToken(uid)` فتسقط بخطأ 500، لأن حساب خدمة تشغيل الدوال (الجيل الثاني) لا يملك دور **Service Account Token Creator** (`iam.serviceAccounts.signBlob`). نفس السبب كان يعطّل `platformImpersonate`.
- **الحل (طُبّق فوراً مع المالك):** مُنح الدور لحساب `623710075919-compute@developer.gserviceaccount.com` عبر `gcloud ... add-iam-policy-binding ... roles/iam.serviceAccountTokenCreator`. **الدخول يعمل الآن.**
- **تحسين دائم (كود):** `finish()` صار يلتقط فشل إصدار الرمز، يسجّله بوضوح في سجلّات الدوال، ويُرجع `{config:true}` تحوّله شاشة القفل إلى رسالة «الدخول بالرمز غير مُفعّل على الخادم بعد» بدل الإيحاء بأن الرمز خاطئ.

### L-2 — [حرج/حيّ] تعطّل Firestore الداخلي يُجمّد التطبيق («INTERNAL ASSERTION FAILED ca9/b815»)
`src/lib/firebase.js:31` يستخدم `persistentMultipleTabManager()`؛ ومع مثيلَي Firestore على نفس الأصل (صفحة إدارة تفتح إطار معاينة يشغّل Firestore ثانياً) + تراكم إعادات التحميل، تتلف قاعدة IndexedDB ويدخل طابور Firestore حالة فاشلة دائمة تُسقط كل عملية لاحقة.
- **الحل (طُبّق):** مُعالج تعافٍ ذاتي في `src/main.jsx` يرصد الرسالة بالضبط: إعادة تحميل عند أول ظهور، فمسح كاش Firestore وإعادة تحميل عند التكرار، وتوقّف عند الثالثة (بلا حلقة)، مع تصفير السلّم بعد ٨ ثوانٍ تشغيل نظيف. الحل الفوري للكاش المتلف مسبقاً كان «Clear site data».

---

## المرحلة أ — أخطاء الطلبات والمال (P0، أعلى خطورة)

### A-1 — [حرج · مؤكّد · أُصلح] «بعث» طلب مُنتهٍ (Resurrection)
`src/lib/db.js:687` — بعد تحويل `updateOrderStatus` من معاملة خادم إلى `updateDoc` معوّض الكمون، لم يعد فيه أي حارس على الحالة. **السيناريو:** جهاز A دون اتصال يضغط «تقدّم» على طلب لقطته قديمة → يُصفّ الكتابة. أثناء ذلك يُلغي جهاز B الطلب أو يُحصّله. عند عودة A للاتصال تهبط كتابته وتُعيد طلباً ملغى/مدفوعاً/مسترجعاً إلى حالة نشطة (المطبخ يطبخ طلباً ملغى؛ طلب مدفوع يصبح قابلاً للتحصيل ثانية). هذا خطر **جديد**: المعاملة القديمة كانت ترفض فوراً دون اتصال، فلا تصطفّ كتابة قديمة.
- **الإصلاح:** حارس في `firestore.rules` (كتلة `orders/{id}`): طلب في حالة `paid/cancelled/refunded` لا يجوز نقله إلى حالة نشطة إطلاقاً — إما يبقى، أو `paid → refunded` فقط (الانتقال المشروع الوحيد). الكتابة المتفائلة القديمة تُرفض على الخادم وتتراجع محلياً. لا كمون على المسار السعيد.
- تحقّقت أن الحارس لا يكسر: الاسترجاع (`paid→refunded`)، المجاملة (الحالة ثابتة)، الدفع الجزئي، تسوية COD، الإلغاء (`active→cancelled`).

### A-2 — [متوسط · مؤكّد · أُصلح] ازدواج طلب على مسار الإنشاء السريع
`src/components/CashierPOS.jsx:377` — `Promise.all([res.done, settle])` كان يحفظ السلة في «المعلّقة» عند أي فشل. لكن إن نجح الإنشاء (`res.done`) وفشل التحصيل فقط، فالطلب **موجود** في Firestore، فإعادة إرساله من «المعلّقة» تُنشئ طلباً ثانياً مكرراً.
- **الإصلاح:** فصل معالجة الفشل — فقط فشل **الإنشاء** يحفظ في «المعلّقة» (آمن، لا شيء هبط)؛ فشل التحصيل فقط يُظهر «أُنشئ الطلب لكن لم يُقبل تلقائياً — اقبله من اللوحة» بلا حفظ (لا ازدواج).

### A-3 — [منخفض · مؤكّد · تُرك موثّقاً] طاولة شبح عند tableId محذوف
`functions/index.js:748` — كتابة الإشغال للضيوف تستخدم `set({},{merge:true})`؛ إن كان tableId لطاولة محذوفة تُنشئ مستنداً شبحاً بحقل إشغال فقط. **غير مرئي** لـ `watchTables` (يرتّب بـ createdAt المفقود)، فمجرد نفاية بيانات لا عطل مرئي. تُرك (إصلاحه يكلّف قراءة إضافية في كل طلب).
- **تحقّق آمن:** مُشغِّل الإشغال idempotent على إعادة المحاولة (set+merge، يُعاد حسابه) — لا ازدواج.

---

## المرحلة ب — جلسات PIN والصلاحيات (دقّقها وكيلان مستقلان تحقّقا من بعضهما)

### B-1 — [عالٍ · مؤكّد · أُصلح] تبديل الجلسة يُوسم قبل نجاحه
`src/lib/auth.jsx:168` — `pinLogin` كان يستدعي `markUnlocked` و`setPinActor` **قبل** انتظار `signInWithCustomToken`. عند فشل التبديل (دون اتصال/رمز مُبطَل) يبقى الجهاز موسوماً «مفتوح كالموظف الجديد» بينما جلسة Firebase لا تزال للمستخدم السابق — فيعمل الجهاز بهوية ويُنسب كل طلب لأخرى، وتتجمّد شاشة القفل على «جارٍ التحقق».
- **الإصلاح:** التبديل أولاً داخل `try/catch`؛ الوسم فقط عند النجاح؛ الفشل يُرجع `{swapFailed:true}` فتُظهر الشاشة إعادة المحاولة.

### B-2 — [عالٍ · مؤكّد · أُصلح] رمز مكرّر قديم يفتح حساب موظف آخر صامتاً
`functions/staffSecurity.js:193` — عند تكرار رمز بين موظفَين قديمَين، كان مسار الاختيار يكتب `pins[hash]=staffId` في فهرس الفرادة، فيصبح المسار السريع يحلّ رمز الجميع لهذا الموظف الواحد — فالموظف الآخر يُسجّل دخوله بهويته وصلاحياته ونسبة طلباته.
- **الإصلاح:** حذف كتابة الفهرس نهائياً من مسار الاختيار — الرمز المكرّر يظلّ يطلب الاختيار في كل مرة حتى يجعله المدير فريداً (`setStaffPinSecure` يفرض الفرادة).

### B-3 — [متوسط · مؤكّد · أُصلح] عدّادات المحاولات غير ذرّية
`functions/staffSecurity.js` — عدّاد الفشل لكل موظف وللمنشأة كان قراءة-تعديل-كتابة، فالطلبات المتزامنة تتجاوز الحدّ (١٠٠ محاولة متوازية تزيد العدّاد ~١). حُوّلا إلى معاملات Firestore (ذرّية).

### B-4 — [متوسط · مؤكّد · أُصلح] إلغاء تفعيل موظف يُسمّم فهرس الفرادة
`setStaffActive` يقلب `active:false` فقط دون كنس رمزه من الفهرس، فلا يستطيع المدير إعادة رمز موظف مغادر («pin-taken» لرمز لا يستخدمه أحد). **الإصلاح:** `setStaffPinSecure` صار يسمح بالكتابة فوق رمز مربوط بموظف مُوقَف/محذوف (يقرأ حالته ويحذف سجلّه القديم).

### B-5 — [متوسط · مؤكّد · أُصلح] مسار staffId يتجاوز عدّاد المنشأة + إصلاح المسار المقفول
`functions/staffSecurity.js` — مسار `staffId` كان يشحن عدّاد الموظف فقط (تجاوز تجميد المنشأة لهجوم على هدف واحد) → صار يشحن عدّاد المنشأة أيضاً عند الفشل. كما أن سجلّاً مقفولاً في المسار السريع كان يُعامَل كخريطة قديمة فيُحذف ويُشحن العدّاد على رمز صحيح → صار المسار السريع يُرجع القفل مباشرة.

### B-6 — [منخفض · مؤكّد · أُصلح] سباق توليد سرّ `_meta` + تسريب البريد
`getPinMeta` توليد السرّ لأول مرة صار في معاملة (يفوز سرّ واحد). ورد الاختيار الغامض صار يُرجع الاسم فقط لا البريد (لا تسريب لمستدعٍ غير موثّق).

### B-7 — [منخفض · مؤكّد · أُصلح] إدخال مزدوج + رسالة الحساب الموقوف
`src/components/PinLock.jsx` — حارس re-entrancy (`submitting` ref) يمنع الضغطة الخامسة من إعادة الإرسال؛ والحساب الموقوف (رمز صحيح) صار في فرعه المنفصل (لا يزيد عدّاد المحاولات ولا اهتزاز الخطأ).

### B-8 — [متوسط · تُرك موثّقاً] تكلفة المسح + DoS التجميد + غياب App Check
مسح السجلّات غير الموثّق يكلّف scrypt لكل موظف؛ وتجميد المنشأة (١٠ محاولات) قابل لإساءة تُعطّل الدخول بالرمز ١٢٠ ثانية (لكن الدخول بالبريد يبقى). **تُرك:** يتطلب App Check (بنية غير منشورة). موثّق كخطر متبقٍّ.

### B-9 — [متوسط · قرار منتج · تُرك] `logout` لا يستدعي `forgetDevice`
بعد الخروج يبقى اسم المنشأة والفريق واختصار PIN على الجهاز. **مقصود** لأجهزة التابلت المشتركة (جوهر الميزة)؛ نسيانه يكسر التدفّق. تُرك عمداً — يمكن لاحقاً إضافة إجراء «هذا جهازي الشخصي».

---

## المرحلة ج — الطاولات والقواعد والإشعارات

### C-1 — [تحقّق آمن] تشديد قراءة الشكاوى لا يكسر المشرف
تحقّقت أن `supervisor` يملك `view_complaints` افتراضياً، فتشديد `firestore.rules` (isStaff → hasCap) لا يعطّل صفحة الشكاوى له؛ والكاشير/النادل لا يملكون صفحة شكاوى أصلاً ومراقب الشارة مُحصّن بردّ فارغ.

### C-2 — [عالٍ · مؤكّد · أُصلح] نافذة القبول تختفي من كل الشاشات بعد التنقّل
`src/components/IncomingAlerts.jsx:42` — انتخاب المثيل الأساسي كان يُثبَّت مرة عند التركيب (`mountCount===1`). عند التنقّل بين صدفتين (يبقى شجرة الصدفة الخارجة مركّبة أثناء تركيب الداخلة) يُثبَّت الناجي على `primary=false` فيرسم null — **فتختفي نافذة قبول الطلبات والمناداة من كل الشاشات حتى إعادة تحميل**.
- **الإصلاح:** انتخاب **تفاعلي**: كل مثيل يسجّل معرّفاً ويُعيد اشتقاق «الأساسي = أصغر معرّف حيّ» عند أي تغيّر في المجموعة الحيّة.

### C-3 — [متوسط · مؤكّد · أُصلح] دفع الطلب الجديد يُسقط عند خطأ عابر
`functions/index.js:42` — `pushToStaff` عند فشل قراءة قائمة الموظفين كان «fail-closed» فيُسقط أهمّ دفع (طلب جديد). **الإصلاح:** التفريق — دفعات المدراء فقط (تحمل أرقاماً مالية) تبقى fail-closed؛ الدفعات التشغيلية المُقيّدة بصلاحية (طلب/حجز/مخزون) تصير fail-open (إرسال غير مُفلتر عند الخطأ العابر).

### C-4 — [متوسط · مؤكّد · أُصلح] رنين متكرّر أثناء التصغير + فهرس الطابور
`IncomingAlerts` — الرنين كل ٤٥ث كان يستمر أثناء التصغير (لا كتم في الحبّة) → صار صامتاً أثناء التصغير (التصغير نفسه إشارة «أنا منتبه»). و`idx` لم يكن يُقصَر عند تقلّص الطابور (أزرار التنقّل تبدو معطّلة) → أُضيف تقصير.

### C-5 — [منخفض · مؤكّد · أُصلح] وميض عدّاد الجرس قبل المصادقة + ازدواج البثّ القديم
`StaffBell.jsx` — عدّاد غير المقروء يُحسب أثناء `uid=''` بمفتاح الجهاز (أعلى من مفتاح المستخدم) فيومض رقماً مضخّماً → لا يُحسب حتى يصل uid. و`notifications.js` كان يفتح بثّ طلبات ثالثاً موازياً → حُوّل إلى تدفّق `liveBoard` المشترك (بثّ واحد لكل منشأة، بلا تغيير سلوك).

### C-6 — [تحقّق آمن — ليست أعطالاً]
- معاينة POS في الإعدادات لا تُعطّل الإشعارات (معزولة في iframe → مثيل `alertsBus` منفصل) — أكّده وكيل + تحليلي.
- عدّاد `liveBoard` صحيح على التبديل السريع (لا تسريب مستمعات).
- `_ROLE_CAPS` مُصدّر فعلاً من `staffSecurity.js`.
- تبديل `push.js` يُعيد تسجيل رمز FCM باسم المستخدم الجديد صحيحاً.
- فحص العضوية عبر المنشآت (`finish`) موجود ويمنع رمز عبر-منشأة.

---

## إصلاحات واجهة (تراجعات من جولة التطوير — بلّغ عنها المالك)

- **بطاقات الكاشير المنهارة إلى شرائح:** `content-visibility:auto` + `aspect-ratio` كان يُقلّص كل بطاقة إلى شريحة على WebView التابلت. أُزيل نهائياً؛ ارتفاع صورة ثابت + بطاقة لا تنهار. أُعيد تصميم السعر شارة ملحومة بزاوية الصورة السفلية اليسرى مع تدرّج للوضوح — وحُلّ قصّها بجعل البطاقة `overflow:visible`→ ثم بنية `pos-tile-photo` تحوي الصورة والشارة.
- **صور نافذة الطلب:** أُزيل الإطار/الخلفية → صورة الطبق مقصوصة على شفافية مثل المنيو (`object-fit:contain`, `:has(img)`).
- **زرّا الطي:** أُخفي الزرّ الصغير داخل القائمة (يبقى زرّ الشريط ٤٤px).
- **زر التثبيت في الشريط المطوي:** أُخفي عند الطي (موجود في بطاقة الشريط والإعدادات).
- **مخطط الطاولات تغطّيه القائمة:** خُفّض أرضية مقياس الاحتواء (0.85→0.68) ليظهر آخر صف طاولات كاملاً.

---

## قائمة النشر المحدّثة (بالترتيب — الدوال أولاً إلزامياً)

1. **IAM (تمّ):** دور `Service Account Token Creator` لحساب تشغيل الدوال — يفكّ الدخول بالرمز.
2. `firebase deploy --only functions` — `pinSignIn` (المعاملات الذرّية + حذف كتابة الفهرس + fail-open للدفع + سجلّ فشل الرمز)، `pushToStaff`.
3. `firebase deploy --only firestore:rules` — **حارس عدم بعث الطلب A-1** + تشديد الشكاوى + قاعدة إشغال الطاولة.
4. `firebase deploy --only firestore:indexes` — الفهرس المركّب orders(tableId, status).
5. نشر الاستضافة — التعافي الذاتي لـ Firestore + إصلاحات الواجهة + بنية تطبيق الموظفين.

## مخاطر متبقّية موثّقة (تحتاج قراراً أو بنية)
- App Check لتقوية `pinSignIn` ضد المسح/DoS (B-8).
- `logout` لا ينسى الجهاز — قرار «مشترك مقابل شخصي» (B-9).
- نفاية طاولة شبح على tableId محذوف — غير مرئية (A-3).
- `persistentMultipleTabManager` هو المُطلِق الجذري لـ L-2؛ التعافي الذاتي يعالج العرَض. يمكن لاحقاً التحويل لوضع تبويب-واحد لإزالته من الجذر (قرار سلوكي).
